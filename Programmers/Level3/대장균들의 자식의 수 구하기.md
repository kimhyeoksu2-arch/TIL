## LEFT JOIN
부모 노드가 가진 자식 노드의 수를 구하기 위해서는 셀프 조인 중에서도 LEFT JOIN을 해야한다.
E1에 있는 모든 행을 왼쪽에 세워두고, E2에서 PARENT_ID가 내 ID와 일치하는 애들을 옆에 가져다 붙인다.

## 윈도우 함수
수평적 관계를 묻는 문제였다면 LEAD() 나 LAG()를 사용하는게 좋지만 
수직적 부모 자식 관계를 묻는 문제였고 갯수를 세야하기 때문에 COUNT() 함수를 사용한다.
